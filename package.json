{
  "name": "create-editor",
  "private": true,
  "description": "package for js candidate test",
  "main": "index.js",
  "scripts": {
    "start": "nodemon ./src/server.js",
    "build:html": "gulp build:html",
    "watch:html": "gulp watch:html",
    "build:css": "gulp build:css",
    "watch:css": "gulp watch:css",
    "build:js": "gulp build:js",
    "watch:js": "gulp watch:js",
    "build": "gulp build",
    "watch": "gulp watch",
    "rebuild": "rm -fR build; gulp build"
  },
  "dependencies": {
    "express": "^4.13.4",
    "jquery": "^2.2.4",
    "junk": "^1.0.2",
    "multer": "^1.1.0",
    "nodemon": "^1.9.1"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-cli": "^1.2.2",
    "gulp-concat": "^2.6.1"
  }
}
